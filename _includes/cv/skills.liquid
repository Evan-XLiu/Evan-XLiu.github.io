{% comment %}
  Skills: 支持两种写法
  1. 直接列举：- "Skill A" / - "Skill B" → 只显示技能名，无 name/level
  2. 带分类：- name: xxx  level: xxx  keywords: [...] → 显示 name (level): keywords
{% endcomment %}

<div class="card-text font-weight-light">
  {% for entry in entries %}
    {% if entry.name %}
      <div class="skill-item">
        {% if entry.icon %}
          <i class="{{ entry.icon }}"></i>
        {% endif %}
        <strong>
          {{- entry.name -}}
          {%- if entry.level %} ({{ entry.level }}){% endif %}:</strong
        >
        {% if entry.keywords %}
          {% if entry.keywords.size > 0 %}
            {% for keyword in entry.keywords %}
              {% if forloop.last %}{{ keyword }}{% else %}{{ keyword }}, {% endif %}
            {% endfor %}
          {% else %}
            {{ entry.keywords }}
          {% endif %}
        {% endif %}
      </div>
    {% else %}
      {% comment %} 直接列举：entry 为字符串，或仅有关键词 {% endcomment %}
      <div class="skill-item">
        {% if entry.keywords %}
          {% if entry.keywords.size > 0 %}
            {% for keyword in entry.keywords %}
              {% if forloop.last %}{{ keyword }}{% else %}{{ keyword }}, {% endif %}
            {% endfor %}
          {% else %}
            {{ entry.keywords }}
          {% endif %}
        {% else %}
          {{ entry }}
        {% endif %}
      </div>
    {% endif %}
  {% endfor %}
</div>

<style>
  .skill-item {
    padding: 0.5rem 0;
  }
</style>
